/**
 * @file 인증 레이아웃
 * @description 로그인/회원가입 페이지의 공통 레이아웃
 *
 * @file Auth Layout
 * @description Shared layout for login and register pages
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
